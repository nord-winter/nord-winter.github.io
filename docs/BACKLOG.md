# Backlog: future work, not touching the current site

Items here are explicitly not on the current blue/light site. Most of it belongs to the future dark/red "persona" side (security/CTF-flavored content), which was deliberately kept out of the main site this whole build. Not ready, not scoped, not a priority until said otherwise.

## Intel-Hub (Discord server)

Repo: https://github.com/nord-winter/intel-hub

His Discord server. Automation, fact-checking, cybersec/livehack utilities, announcements pulled from CTF/HTB/THM/Google security events. His idea: use it to pull people together around CTF, build a team, train and grow together.

He floated dedicating a block or a page on the site to this. Not built. When it's time: this is red-side content (CTF/HackTheBox/TryHackMe), same bucket as the deferred dark/red persona toggle from the original site plan, not something that belongs on the current professional/consulting-facing site without that toggle existing first.

## bkkhackerspace

Domain already purchased. Still fully in planning, nothing built yet, waiting on a colleague to actually stand up the infrastructure. Nothing to reference or link from the site until it exists.

## Older deferred items (from earlier in the build)

- Dark/red "persona" side: a second palette + toggle-switchable content bucket for security/red-team-flavored material (CTF placements, TryHackMe/Cisco certs already sitting unused in `config.ts`'s `certifications` array, HackTheBox-style entries). Never built, was explicitly kept out of the current site's scope from the start.
- Brochure/PDF one-pager, see `BROCHURE-BRIEF.md`.
- Anti-AI-crawler / invisible-text protection for a real photo on the profile page, if one gets added.
- Real prices for the 4 consulting services (currently `On_Request`).
- `intel-hub`/CTF community page above.
